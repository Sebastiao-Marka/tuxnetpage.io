import 'package:flutter/material.dart';

class TuxCores {
  static final Color tux_roxo = const Color.fromARGB(255, 102, 23, 121);
  static final Color tux_amarelo =
      Color(const Color.fromARGB(255, 241, 147, 6).value);
  static final Color tux_preto =
      Color(const Color.fromARGB(255, 0, 0, 0).value);
  static final Color tux_banco =
      Color(const Color.fromARGB(255, 255, 255, 255).value);
  static final Color tux_banco_cina =
      Color(const Color.fromARGB(255, 241, 240, 240).value);
}
